extends layout

block content
  h1= title
  div.profile.center
    img(src=user.photo style='width:150px; border-radius:25%')
    h3= user.displayName
    p #{JSON.stringify(user)}
    a(href='/logout') Logout